# VERSION_HISTORY 版本管理

## 当前版本定位

当前项目处于：V4第二聊天窗口。

这不是一个单独的软件发布号，而是当前协作阶段的记录方式。它表示：项目已经有 V3 稳定业务流程，同时正在推进 V4 结构化订单和规则渲染方向，并开始建立长期维护用的项目管理文档体系。

## v1 演进思路

主要目标：先跑通“聊天内容到订单信息”的最小流程。

主要解决的问题：

- 建立基础后端服务。
- 支持输入客户聊天内容。
- 尝试用 AI 提取订单字段。
- 生成基础 Excel 文件。

特点：

- 功能以验证可行为主。
- 结构较简单，适合快速试错。

## v2 演进思路

主要目标：让系统从演示流程变成可配置流程。

主要解决的问题：

- 增加字段库。
- 增加模板映射。
- 增加配置中心。
- 支持不同 Excel 模板。
- 改善字段确认和导出流程。

特点：

- 开始区分“业务数据”和“模板配置”。
- 用户可以通过配置适配不同订单模板。

## v3 演进思路

主要目标：形成相对稳定的日常订单生成流程。

主要解决的问题：

- 文档编号和产品编码规则。
- 产品描述模板。
- 图片字段和图片素材池。
- Layout Engine 排版。
- 本地输出同步。
- AI 设置保存和锁定。
- 首页和配置中心的主要工作流稳定。

特点：

- V3 是当前正式业务流程的主要基础。
- 修改时应优先保持兼容，不适合一次性大重构。

## v4 演进思路

主要目标：从“字段映射驱动”升级到“产品结构驱动”和“规则渲染驱动”。

主要解决的问题：

- 用 Product Schema 描述产品结构。
- 用 Example Order 表示真实订单样例。
- 用 Validator 检查结构是否正确。
- 用 Renderer 转换成可渲染字段。
- 用 Excel Render Rules 描述写入规则。
- 用 Rule Preview 和 Rule Executor 验证 Excel 输出链路。

当前阶段：

- 当前版本定位为 V4第二聊天窗口。
- V4 已经有实验页面 `/v4-schema`。
- V4 已有 Schema、Example、Renderer、Excel Rules、Rule Executor 等模块。
- V4 还没有完全接入 V3 首页正式订单生成流程。

## 后续版本记录格式

以后新增版本时，建议按以下格式记录：

```text
## vX 演进思路

主要目标：

主要解决的问题：

- 

完成内容：

- 

仍未完成：

- 

注意事项：

- 
```

最后更新：V4第二聊天窗口
